<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sportstelling.sign.dao.SignDAO">
	<!-- 회원가입 -->
	<insert id="insertSign" parameterType="map">
		INSERT INTO `user`
		(
			`loginId`
			, `password`
			, `name`
			, `nickName`
			, `email`
			, `createdAt`
			, `updatedAt`
		) VALUES (
			#{loginId }
			, #{password }
			, #{name }
			, #{nickName }
			, #{email }
			, now()
			, now()
		);
	</insert>
	<!-- ID 중복체크 -->
	<select id="selectCountById" resultType="int" parameterType="string">
		SELECT
			count(*)
		FROM
			`user`
		WHERE
			`loginId` = #{loginId };
	</select>
	<!-- 닉네임 중복체크 -->
	<select id="selectCountByNickName" resultType="int" parameterType="string">
		SELECT
			count(*)
		FROM
			`user`
		WHERE
			`nickName` = #{nickName };
	</select>
	<!-- 로그인 -->
	<select id="selectSignByLoginIdPassword" resultType="com.sportstelling.sign.model.Sign"
	parameterType="map">
		SELECT
			`id`
			, `loginId`
			, `password`
			, `name`
			, `nickName`
			, `email`
			, `createdAt`
			, `updatedAt`
		FROM
			`user`
		WHERE
			`loginId` = #{loginId }
			AND `password` = #{password };
	</select>
	<!-- 아이디 찾기 -->
	<select id="selectByNameEmail" resultType="int">
		SELECT
			`id`
			, `loginId`
			, `password`
			, `name`
			, `nickName`
			, `email`
			, `createdAt`
			, `updatedAt`
		FROM
			`user`
		WHERE
			`name` = #{name }
			AND `email` = #{email };
	</select>
</mapper>